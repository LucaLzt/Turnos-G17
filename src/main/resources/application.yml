spring:
 datasource:
  url: jdbc:mysql://localhost:3306/obj2_tp
  username: root 
  password: root
 jpa:
  show-sql: true
  hibernate:
   ddl-auto: update
  properties:
   dialect: org.hibernate.dialect.MySQL5Dialect
 thymeleaf:
  cache: false
 mail:
  host: smtp.gmail.com
  port: 587
  username: root
  password: root
  properties:
   mail:
    smtp:
     auth: true
     starttls:
      enable: true
      required: true