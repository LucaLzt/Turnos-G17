spring:
 datasource:
  url: ${DB_URL}
  username: ${USERNAME}
  password: ${PASSWORD}
 jpa:
  show-sql: true
  hibernate:
   ddl-auto: create # cambiar por update cuando esté todo terminado
  properties:
   dialect: org.hibernate.dialect.MySQL5Dialect
 thymeleaf:
  cache: false
